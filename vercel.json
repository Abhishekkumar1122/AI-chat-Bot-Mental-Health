{
  "buildCommand": "cd client && npm install && VITE_API_URL=$VITE_API_URL npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
